import React from 'react'
import styled, {css} from 'styled-components';

const HeroSection = styled.section`
  
`;

const Hero = () => {
    return (
        <div></div>
    )
}

export default Hero;